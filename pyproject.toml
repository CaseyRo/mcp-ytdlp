[project]
name = "mcp-ytdlp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastmcp>=3.0.2,<4.0.0",
  "yt-dlp>=2026.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
